# Tasks
- Configure environment variables in `.env.local` for Supabase credentials.
- Run `npm --prefix frontend install` to install dependencies.
- Start development server with `npm --prefix frontend run dev`.
- Deploy to Cloudflare using `scripts/deploy-cloudflare.sh` after setting `CLOUDFLARE_ACCOUNT_ID` and `CLOUDFLARE_API_TOKEN`.

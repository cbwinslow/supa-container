# -----------------------------------------------------------------------------
# Environment Variables Template for Supabase Super Stack
# -----------------------------------------------------------------------------
# Copy this file to a new file named '.env' (i.e., `cp .env.example .env`)
# and fill in the values for your specific environment.
#
# NEVER commit the .env file to version control.
# -----------------------------------------------------------------------------

# --- Domain and Traefik ---
# Your public domain name.
DOMAIN=your-domain.com
# Your email for Let's Encrypt SSL certificates.
LETSENCRYPT_EMAIL=your-email@your-domain.com
# A securely generated hash for the Traefik dashboard password.
# You can generate this with: htpasswd -nb admin 'your-secure-password'
TRAEFIK_ADMIN_PASSWORD_HASH=admin:$apr1$....

# --- Supabase ---
# These keys are obtained from your Supabase project dashboard
# or after running 'supabase start' for the first time locally.
# Public-facing key for the client-side application.
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
# Secret key for server-to-server communication (never expose this publicly).
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key
# The secret for signing JWTs, found in your Supabase project's JWT settings.
SUPABASE_JWT_SECRET=your_supabase_jwt_secret

# --- n8n Workflow Automation ---
# Credentials for basic authentication on the n8n dashboard.
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=your_secure_n8n_password

# --- FastAPI Backend ---
# A long, random string used for signing internal tokens or other security functions.
# You can generate one with: openssl rand -hex 32
FASTAPI_SECRET_KEY=your_long_random_fastapi_secret
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
